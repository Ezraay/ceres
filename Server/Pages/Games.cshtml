@page
@model GamesModel
@{
    ViewData["Title"] = "Games on server";
}

<div class="text-center">
    <h1>Welcome to game</h1>
    <h3>@Model.GameId</h3>
    <h4>@Model.Player1?.UserName vs @Model.Player2?.UserName</h4>
</div>